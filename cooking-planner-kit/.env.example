# Server-side only. Never expose to the browser.
ANTHROPIC_API_KEY=

# Supabase (Phase 3+)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Hard monthly spend ceiling for the generation endpoint (in cents).
GENERATION_MONTHLY_CAP_CENTS=50000
